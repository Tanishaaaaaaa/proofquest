{
  "name": "proofquest",
  "version": "1.0.0",
  "description": "Decentralized physical world micro-task marketplace built on Solana",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install && cd ../smart-contract && npm install"
  },
  "keywords": ["solana", "web3", "marketplace", "tasks", "nft"],
  "author": "ProofQuest Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}